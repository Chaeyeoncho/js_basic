/**
 * Naming Conventions
 *
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 모두 사용 할 수 있다.
 * 2) 특수기호는 언더스코와 달러를 사용할 수 있다.
 * 3) 숫자로 이름을 시작할 수 없다.
 * 3) 키워드는 변수명으로 사용할 수 없다.
 *  var const ='var'
 */
let codefactory = "코드팩터리";
var $ive = "아이브";
const _yujin = "안유진";

console.log(codefactory);
console.log($ive);
console.group(_yujin);

//let 1name ='no'; , 변수 자체는 숫자로 X

/***
 * 개발자들끼리 약속한것
 * 1)CamelCase - 대부분의 언어에서 사용 첫번째 글자 소문자로 쓰고 띄어쓰기할시점에 대문자
 * ex)codefactory이면 codeFactory로 쓴다..
 * 2)snake_case 소문자만 있는 경우 띄어쓰기를 하지 않고 ..! 파일이름지을때 , 파이썬 같은 경우
 * 3)PascalCase - 첫번째글자도 대문자 띄어쓰기할 시점에도 대문자를 사용한다 C#같은 계열의 언어에서 사용
 *  하지만 잘 사용 되진 않는다 !
 */
